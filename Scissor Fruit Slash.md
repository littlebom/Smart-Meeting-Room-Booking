# Prompt: เกมกรรไกรตัดผลไม้ (Scissor Fruit Slash) — ฉบับสมบูรณ์ All-in-one

สร้างเกมแนว Fruit Ninja ชื่อ "กรรไกรตัดผลไม้" ที่ควบคุมด้วยท่ามือ "กรรไกร" (นิ้วชี้ + นิ้วกลาง) ผ่านกล้องเว็บแคม ทำงานบนเว็บเบราว์เซอร์ ส่งมอบเป็นไฟล์ HTML ไฟล์เดียวจบที่เปิดเล่นได้ทันที โดยทำให้ครบทุกข้อด้านล่างในครั้งเดียว (ไม่ต้องถามกลับ ไม่ต้องแบ่งเป็นขั้นตอน ห้ามตัดทอนโค้ดด้วย "...") เกมนี้เป็น client-side ล้วน ห้ามเรียกใช้ Gemini API หรือ backend ใด ๆ

================================================================
A) ข้อกำหนดทางเทคนิค (สำคัญมาก ห้ามเปลี่ยน)
================================================================
- เป็นไฟล์ HTML ไฟล์เดียวจบ (single self-contained file) ใช้ vanilla JavaScript ล้วน ไม่มี framework ไม่มี build step ไม่มี npm
- วาดทุกอย่างด้วย HTML5 Canvas เต็มจอ ปรับ devicePixelRatio ให้ภาพคม และจัดการ window.resize (คำนวณตำแหน่งใหม่โดยคงสถานะคะแนน/เวลา/ผลไม้ที่ลอยอยู่ ห้ามรีเซ็ตเกม)
- ใช้ MediaPipe Hands + Camera Utils โหลดผ่าน CDN ของ jsdelivr
- ภาษา UI เป็นภาษาไทย ฟอนต์ Fredoka (หัวข้อ/ตัวเลข) + Noto Sans Thai (เนื้อหา) จาก Google Fonts
- ธีมสีเข้ม (dark): พื้นหลัง #0f172a, ตัวอักษร #f8fafc, สี accent ฟ้า #0ea5e9
- เสียง feedback สร้างด้วย Web Audio API ล้วน (ไม่ใช้ไฟล์เสียงภายนอก) ปลดล็อก AudioContext จาก user gesture ตอนกดปุ่มเริ่ม
- รองรับการควบคุม 2 โหมด: มือ (hand tracking) และเมาส์/ทัช ตามข้อ B-2
- ประสิทธิภาพ: แคชค่า ไม่สร้าง object ใหม่ใน render loop, ใช้ object pool สำหรับ particles ถ้าเหมาะสม
- ใส่คอมเมนต์ภาษาไทยอธิบายแต่ละส่วนของโค้ด

================================================================
B) กล้อง + ตรวจจับท่า "กรรไกร" + จุดตัด
================================================================
- ตั้งค่า MediaPipe Hands: maxNumHands: 1, modelComplexity: 1, minDetectionConfidence: 0.7, minTrackingConfidence: 0.6
- ใช้ Camera จาก camera_utils ส่งภาพ video (640×480) เข้า hands.send() ทุกเฟรม
- ประมวลผลมือ (มือเดียว 21 landmarks):
  - Mirror แนวนอน: x = (1 - p.x) * width, y = p.y * height
  - จุดตัด (cutPoint) = กึ่งกลางระหว่างปลายนิ้วชี้ (landmark 8) กับปลายนิ้วกลาง (landmark 12)
  - Low-pass filter ให้จุดตัดนุ่ม: cutPoint.x += (raw.x - cutPoint.x) * 0.5 (และ y เช่นกัน)
  - เก็บ lastHandSeenTime ถ้าไม่เจอมือเกิน 500ms ถือว่ามือหลุด
- ตรวจท่ากรรไกร:
  - fingerExtended(lm, tip, pip) = dist(lm[0], lm[tip]) > dist(lm[0], lm[pip]) * 1.1
  - ท่ากรรไกร = นิ้วชี้ (8,6) เหยียด + นิ้วกลาง (12,10) เหยียด + นิ้วนาง (16,14) งอ + นิ้วก้อย (20,18) งอ
  - bladeGap = dist(lm[8], lm[12]) / dist(lm[0], lm[9]) (ระยะปลายนิ้วชี้–กลาง เทียบขนาดฝ่ามือ)
- ตรวจจังหวะ "ฉับ" แบบ hysteresis กันกระพริบ: เมื่อเป็นท่ากรรไกร — เริ่มตัด (isCutting = true) เมื่อ bladeGap < 0.45, ปล่อยเมื่อ bladeGap > 0.70 ใช้ edge detection เรียก onSnip() ตอนเริ่มตัดเท่านั้น
- การเรนเดอร์ทุกเฟรมผ่าน requestAnimationFrame:
  - พื้นหลัง: ภาพจากกล้อง (mirror) จาง ๆ (opacity ~0.25) + vignette gradient
  - โครงกระดูกมือจาง ๆ + เน้น "ใบมีด" 2 เส้น (นิ้วชี้ 5→8, นิ้วกลาง 9→12) สีฟ้า #0ea5e9 มี glow เปลี่ยนเป็นสีแดงตอนหุบ (กำลังตัด)
  - จุดตัด: วงแหวนรัศมี CUT_HIT_RADIUS = 55 (คือโซนที่ตัดโดนจริง แสดงให้ผู้เล่นเห็นเพื่อเล็งง่าย) + จุดศูนย์กลาง แฟลชสีตอนฉับ

================================================================
B-2) โหมดการควบคุม 2 แบบ + Fallback อัตโนมัติ (สำคัญ)
================================================================
- มี state inputMode: 'hand' | 'mouse'
- เมื่อกดปุ่มเริ่ม (ใช้กล้อง): ขอสิทธิ์กล้อง
  - ถ้าสำเร็จ → inputMode = 'hand' เล่นด้วยท่ากรรไกรตามข้อ B
  - ถ้าล้มเหลว (NotAllowedError, NotFoundError, NotReadableError หรือ timeout 8 วินาที) → แสดงหน้า "กล้องใช้งานไม่ได้" พร้อม 2 ปุ่ม: [🔄 ลองเชื่อมต่อกล้องอีกครั้ง] และ [🖱 เล่นด้วยเมาส์แทน]
  - กดเล่นด้วยเมาส์ → inputMode = 'mouse' เริ่มเกมทันทีโดยไม่ต้องมีกล้อง
- โหมดเมาส์: เลื่อนเมาส์ = จุดตัด (cutPoint), คลิก = onSnip() รองรับ touch (touchstart = ฉับที่จุดสัมผัส, touchmove = เลื่อนจุดตัด) สำหรับแท็บเล็ตด้วย
- โหมดเมาส์ไม่วาดภาพกล้องพื้นหลังและโครงกระดูกมือ ใช้พื้นหลัง gradient เข้มแทน และ HUD เปลี่ยนคำแนะนำเป็น "คลิกเพื่อตัดผลไม้"
- ระหว่างเล่นโหมด hand ถ้ามือหลุดเกิน 10 วินาทีติดต่อกัน ให้แสดงปุ่มเล็กมุมจอ "สลับไปใช้เมาส์" (กดแล้วเปลี่ยน inputMode ได้ทันที โดยไม่รีเซ็ตคะแนน/เวลา)
- หน้า Start เพิ่มปุ่มรอง "เล่นด้วยเมาส์ (ไม่ใช้กล้อง)" ใต้ปุ่มเริ่มหลัก สำหรับผู้ที่ไม่มีกล้องตั้งแต่แรก

================================================================
C) ผลไม้ + ฟิสิกส์ + การตัด
================================================================
- ชนิดผลไม้ 6 สี: apple #ef4444, orange #fb923c, lime #84cc16, lemon #facc15, grape #a855f7, berry #3b82f6
- spawnFruit(): เกิดจากขอบล่าง (y = height + radius), ความเร็วขึ้น vy = -(14.5 ถึง 18.5), มี vx สุ่ม (เอียงเข้าหากลางจอ), หมุน rot/vr, รัศมีสุ่ม 30–46
- แรงโน้มถ่วง GRAVITY = 0.28 (ผลไม้ลอยขึ้นแล้วตกลง)
- update ทุกเฟรม: x += vx; y += vy; vy += GRAVITY; rot += vr; ลบผลไม้เมื่อตกพ้นจอ
- onSnip(): วนตรวจ fruits ที่ยังไม่ถูกตัด ถ้า dist(cutPoint, fruit) < fruit.radius + CUT_HIT_RADIUS → ตัดสำเร็จ (f.cut = true) + คิดคะแนน
- วาดผลไม้: วงกลม radial gradient (ขาว → สีผลไม้ → เข้ม) + ไฮไลต์ขาว + ใบไม้เขียวด้านบน
- spawn ผลไม้เป็นจังหวะด้วยตัวนับเวลา (หน่วยมิลลิวินาที ไม่ใช้เฟรม เพื่อให้จังหวะเท่ากันทุกจอ) ปรับถี่ขึ้นเล็กน้อยเมื่อเวลาใกล้หมด

================================================================
D) เอฟเฟกต์ + ระบบเสียง (Web Audio API)
================================================================
- particles น้ำผลไม้: อนุภาคกระเด็นสีตามผลไม้ มีแรงโน้มถ่วง ค่า life จางหายไป
- ครึ่งผล (halves): ตอนตัด สร้าง 2 ชิ้นบินแยกซ้าย/ขวา — วาดด้วยการ clip สี่เหลี่ยมครึ่งซ้าย/ขวาทับวงกลม แล้วหมุน + จางหาย
- score popups: ข้อความคะแนน (+10, ★ +50 ฯลฯ) เด้งลอยขึ้นแล้วจาง
- เสียง oscillator สั้น ๆ: ฉับ (square 1100→1400Hz สั้น 2 จังหวะ), ตัดผลไม้ (triangle 523→784Hz), โดนระเบิด (sawtooth/square โทนต่ำ), ผลไม้ทอง (เสียงชิมเมอร์ระยิบ), จบเกม (โน้ตไล่ลง)

================================================================
E) โหมดจับเวลา 20 วินาที + คะแนน + ระเบิด + ผลไม้ทอง
================================================================
- รอบเดียว 20 วินาที (ROUND_TIME_MS = 20000) — เริ่มจับเวลาเมื่อระบบพร้อม (flag systemReady: กล้องพร้อม หรือเริ่มโหมดเมาส์)
- วงแหวนนับถอยหลังกลางบนจอ (สีฟ้า เปลี่ยนเป็นแดงเมื่อเหลือ ≤ 5 วินาที)
- หมดเวลา → status = 'gameover' + บันทึกสถิติสูงสุด (best) ไว้ในตัวแปร (ไม่ใช้ localStorage)
- คะแนนตามขนาด: fruitPoints(f) = max(5, round(60 - f.radius)) → ลูกเล็กได้คะแนนเยอะ ลูกใหญ่ได้น้อย
- โบนัสคอมโบ: ตัดหลายลูกในจังหวะฉับเดียว ได้โบนัสเพิ่ม (count - 1) * 5 + ป๊อปอัป "คอมโบ!"
- ระเบิด 💣: spawnFruit('bomb') (ลูกสีเข้ม + ชนวนไฟ) เริ่มเกิดหลังเกมเดินแล้ว 2.5 วินาที; ตัดโดน = หักคะแนน 15 (BOMB_PENALTY) + แฟลชจอสีแดง + เสียงระเบิด แต่ไม่จบเกม (เล่นต่อจนหมดเวลา คะแนนต่ำสุดคือ 0)
- ผลไม้ทองโบนัส ★: spawnFruit('gold') ลูกเล็ก (รัศมี 28) ลอยเร็วกว่าปกติ เกิดด้วยตัวนับเวลาแยก goldTimer สุ่มทุก 3.5–4.5 วินาที (≈ 4–5 ลูกต่อรอบ); ตัดได้โบนัส 50 (GOLD_BONUS) + ประกายทอง + เสียงชิมเมอร์ + ป๊อปอัป "★ +50"; วาดเป็นวงทองเรืองแสง (glow) + ดาวขาวตรงกลาง
- HUD มุมซ้ายบน: "คะแนน N" (ไม่มีระบบหัวใจ — ใช้เวลาเป็นตัวกำหนดจบเกม)

================================================================
F) หน้าจอต่าง ๆ + ขัดเกลา
================================================================
หน้าจอ overlay (วาดบน canvas, ปุ่มทุกปุ่มกดได้ทั้งเมาส์และการ "ฉับ" — จุดตัดทับปุ่มแล้วฉับ):
- Start (หน้าแรก): ชื่อเกม + วิธีเล่น (ทำมือเป็นกรรไกร ชูนิ้วชี้ + นิ้วกลาง แล้วหุบเพื่อฉับ · มีเวลา 20 วินาที · ลูกเล็กได้คะแนนเยอะ · ★ ผลไม้ทองโบนัส +50 · ระวังระเบิด 💣 หักคะแนน) + ปุ่มหลัก "✂️ เริ่มเล่น (ใช้กล้อง)" + ปุ่มรอง "🖱 เล่นด้วยเมาส์ (ไม่ใช้กล้อง)"
  - flow ปุ่มหลัก: กด → ปลดล็อกเสียง + รีเซ็ตเกม + แสดง Loading (spinner) + ขอกล้อง/โหลด MediaPipe จาก user gesture → พร้อมแล้วซ่อน Loading + systemReady = true → กล้อง error เข้าหน้า fallback ตามข้อ B-2
  - flow ปุ่มรอง: กด → ปลดล็อกเสียง แล้วเริ่มเกมทันทีในโหมดเมาส์
  - กันการกดปุ่ม Start ซ้ำระหว่างโหลดด้วย flag
- Game Over: "หมดเวลา!" + "คะแนน N" + "สถิติสูงสุด N" + ปุ่ม "เล่นอีกครั้ง" (ฉับ/คลิกกดได้ เริ่มรอบใหม่ในโหมดเดิม)
- Edge cases ที่ต้องรองรับ: มือหลุดกลางคัน (แสดงข้อความ "ไม่เห็นมือ ยกมือขึ้นหน้ากล้อง" จาง ๆ), resize ระหว่างเล่น (คงสถานะ), ฉับรัว ๆ (edge detection กันซ้ำ), แท็บสลับไปมา (หยุด timer ด้วย visibilitychange ถ้าทำได้)

================================================================
ค่าคงที่สำคัญ (รวมไว้ด้านบนของสคริปต์ ปรับสมดุลเกมได้ง่าย)
================================================================
- CUT_HIT_RADIUS = 55        // รัศมีโซนตัด (ยิ่งมากตัดง่าย)
- CUT_CLOSE = 0.45           // เกณฑ์หุบใบมีด (เริ่มตัด)
- CUT_OPEN = 0.70            // เกณฑ์กางใบมีด (ปล่อย)
- ROUND_TIME_MS = 20000      // เวลาต่อรอบ
- GRAVITY = 0.28             // แรงโน้มถ่วง
- BOMB_PENALTY = 15          // โทษตัดระเบิด
- GOLD_BONUS = 50            // โบนัสผลไม้ทอง
- GOLD_INTERVAL = 3500–4500ms // ความถี่เกิดผลไม้ทอง (~4–5 ลูก/รอบ)
- fruitPoints(f) = max(5, round(60 - f.radius)) // คะแนนตามขนาด
